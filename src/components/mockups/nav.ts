/** The navigations the screens are drawn inside. */

/** The Portal's own left-hand navigation, as built. */
export const NAV = ['Home', 'Trips', 'Contacts', 'Suppliers', 'Horizon', 'Vault', 'Reports', 'Settings'];

/** What a supplier signs into: their own requests and rates. Horizon is not
    here — a supplier is listed in it, they do not log into it. */
export const SUPPLIER_NAV = ['Home', 'Requests', 'Quotes', 'Bookings', 'Rates', 'Settings'];
